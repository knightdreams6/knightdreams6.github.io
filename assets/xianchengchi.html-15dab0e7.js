import{_ as e,o,c,b as d}from"./app-c5e7af21.js";const a={},n=d(`<h3 id="一、使用线程池的好处" tabindex="-1"><a class="header-anchor" href="#一、使用线程池的好处" aria-hidden="true">#</a> 一、使用线程池的好处</h3><blockquote><p>池化技术：线程池、数据库连接池、Http连接池等等都是对这个思想的应用。</p><p><strong>池化技术的思想主要是为了减少每次获取资源的消耗，提高对资源的利用率。</strong></p></blockquote><ul><li><strong>降低资源消耗</strong>。通过重复利用已创建的线程减低线程的创建和销毁造成的消耗。</li><li><strong>提高响应速度</strong>。当任务到达时，任务可以不需要等待线程的创建就能立即执行。</li><li><strong>提高线程的可管理性</strong>。线程是稀缺资源，如果无限制的创建，不仅会消耗系统资源，还会降低系统的稳定性，使用线程池可以进行统一的分配，调优和监控。</li></ul><hr><h3 id="二、executor框架" tabindex="-1"><a class="header-anchor" href="#二、executor框架" aria-hidden="true">#</a> 二、Executor框架</h3><h4 id="_1、简介" tabindex="-1"><a class="header-anchor" href="#_1、简介" aria-hidden="true">#</a> 1、简介</h4><p>Executor框架是 java5 之后引进的，在 Java 5 之后，通过 Executor 来启动线程比使用 Thread 的 start 方法更好，除了更易管理，效率更好（用线程池实现，节约开销）外，还有关键的一点：有助于避免 this 逃逸问题。</p><blockquote><p>补充：this逃逸是指在构造函数返回之前其他线程就持有该对象的引用，调用尚未构造完全的对象的方法可能会引发令人疑惑的错误。</p></blockquote><p>Executor框架不仅包括了线程池的管理，还提供了线程工厂、队列以及拒绝策略等，Executor框架让并发编程变的更加简单。</p><h4 id="_2、executor框架结构" tabindex="-1"><a class="header-anchor" href="#_2、executor框架结构" aria-hidden="true">#</a> 2、Executor框架结构</h4><ol><li><p>任务（Runnable/Callable）</p><blockquote><p>执行任务需要实现的 <code>Runnable</code> 接口或 <code>Callable</code> 接口。这两个接口的实现类都可以被 <code>ThreadPoolExecutor</code> 或 <code>ScheduledThreadPoolExecutor</code> 执行</p></blockquote></li><li><p>任务的执行（Executor）</p><blockquote><p>任务执行机制的核心接口<code>Executor</code>，以及继承自<code>Executor</code>接口的<code>ExecutorService</code>接口。<code>ThreadPoolExecutor</code> 和 <code>ScheduledThreadPoolExecutor</code> 这两个关键类实现了 <code>ExecutorService</code>接口。</p></blockquote><p><code>ThreadPoolExecutor</code> 这个类，这个类在我们实际使用线程池的过程中，使用频率非常高。</p></li><li><p>异步计算的结果（Future）</p><blockquote><p><code>Future</code>接口以及该接受的实现类<code>FutureTask</code>都可以代表异步计算的结果。</p><p>当我们把<code>Runnable</code>接口或<code>Callable</code>接口的实现类提交给<code>ThreadPoolExecutor</code>或<code>ScheduledThreadPoolExecutor</code>执行。调用<code>submit()</code>方法时会返回一个<code>FutureTask</code>对象</p></blockquote></li></ol><hr><h3 id="三、线程池实现类-threadpoolexecutor" tabindex="-1"><a class="header-anchor" href="#三、线程池实现类-threadpoolexecutor" aria-hidden="true">#</a> 三、线程池实现类 <code>ThreadPoolExecutor</code></h3><h4 id="_1、threadpoolexecutor-类分析" tabindex="-1"><a class="header-anchor" href="#_1、threadpoolexecutor-类分析" aria-hidden="true">#</a> 1、<code>ThreadPoolExecutor</code> 类分析</h4><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code>    <span class="token keyword">public</span> <span class="token class-name">ThreadPoolExecutor</span><span class="token punctuation">(</span><span class="token keyword">int</span> corePoolSize<span class="token punctuation">,</span>    <span class="token comment">// 线程池的核心线程数量</span>
                              <span class="token keyword">int</span> maximumPoolSize<span class="token punctuation">,</span> <span class="token comment">// 线程池的最大线程数 </span>
                              <span class="token comment">// 当线程数大于核心线程数多余的空闲线程存活的最长时间</span>
                              <span class="token keyword">long</span> keepAliveTime<span class="token punctuation">,</span> 
                              <span class="token comment">// 时间单位</span>
                              <span class="token class-name">TimeUnit</span> unit<span class="token punctuation">,</span>
                              <span class="token comment">// 任务队列，用来存储等待执行任务的队列</span>
                              <span class="token class-name">BlockingQueue</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Runnable</span><span class="token punctuation">&gt;</span></span> workQueue<span class="token punctuation">,</span>
                              <span class="token comment">// 线程工厂，用来创建线程，一般默认即可</span>
                              <span class="token class-name">ThreadFactory</span> threadFactory<span class="token punctuation">,</span>
                      <span class="token comment">// 拒绝策略，当提交任务过多而不能及时处理时，我们可以定制策略来处理任务</span>
                              <span class="token class-name">RejectedExecutionHandler</span> handler<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>ThreadPoolExecutor</code> <strong>三个最重要的参数：</strong></p><ol><li><code>corePoolSize</code>：核心线程数定义了最小可同时运行的线程数量</li><li><code>maximumPoolSize</code>: 当队列中存放的任务达到队列容量的时候，当前可以同时运行的线程数量变为最大线程数</li><li><code>workQueue</code>:当新任务来的时候回显判断当前运行的线程数量是否达到核心线程数，如果达到的话，新任务就会被存放在队列中。</li></ol><p><strong>其他参数：</strong></p><ol><li><code>keepAliveTime</code>:当线程池中线程数量大于<code>corePoolSize</code>的时候，如果这时没有新的任务提交，核心线程外的线程不会立即销毁，而是会等待，直到等待的时间超过了<code>keepAliveTime</code>才会被回收销毁；</li><li><code>unit</code>: <code>keepAliveTime</code>参数的时间单位</li><li><code>threadFactory</code>: executor创建新线程的时候会用到</li><li><code>handler</code>:饱和策略，如果当前同时运行的线程数量达到最大线程数量并且队列也已经放满了任务时，<code>ThreadPoolTaskExecutor</code>定义了一些策略： <ul><li><code>AbortPolicy</code>:抛出<code>RejectedExecutionExeception</code>来拒绝新任务的处理（默认）</li><li><code>CallerRunsPolicy</code>:调用执行自己的线程运行任务。这种策略会降低新任务的提交速度，影响程序的整体性能。另外，这个策略喜欢增加队列容量。如果您的应用程序可以承受此延迟并且不能丢弃任何一个任务请求的话，可以选择此策略</li><li><code>DiscardPolicy</code>: 不处理新任务，直接丢弃掉。</li><li><code>DiscardOldestPolicy</code>: 此策略将丢弃最早的未处理的任务请求</li></ul></li></ol><h4 id="_2、-推荐使用threadpoolexecutor构造函数创建线程池" tabindex="-1"><a class="header-anchor" href="#_2、-推荐使用threadpoolexecutor构造函数创建线程池" aria-hidden="true">#</a> 2、 推荐使用<code>ThreadPoolExecutor</code>构造函数创建线程池</h4><p>在《阿里巴巴 Java 开发手册》“并发处理”这一章节，明确指出线程资源必须通过线程池提供，不允许在应用中自行显示创建线程。</p><blockquote><p>使用线程池的好处是减少在创建和销毁线程上所消耗的时间以及系统资源开销，解决资源不足的问题。如果不使用线程池，有可能会造成系统创建大量同类线程而导致消耗完内存或者“过度切换”的问题。</p></blockquote><p>另外《阿里巴巴 Java 开发手册》中强制线程池不允许使用 Executors 去创建，而是通过 <code>ThreadPoolExecutor</code> 构造函数的方式，这样的处理方式让写的同学更加明确线程池的运行规则，规避资源耗尽的风险。</p><p>Executors 返回线程池对象的弊端</p><ul><li><code>FixedThreadPool</code> 和 <code>SingleThreadExecutor</code> : 允许请求的队列长度为 <code>Integer.MAX_VALUE</code> 可能堆积大量的请求，从而导致 OOM。</li><li><code>CachedThreadPool</code> 和 <code>ScheduledThreadPool</code> ：允许创建的线程数量为 <code>Integer.MAX_VALUE</code> ，可能会创建大量线程，从而导致 OOM。</li></ul><hr><h3 id="四、几个常见的对比" tabindex="-1"><a class="header-anchor" href="#四、几个常见的对比" aria-hidden="true">#</a> 四、几个常见的对比</h3><h4 id="_1、runnable-与-callable" tabindex="-1"><a class="header-anchor" href="#_1、runnable-与-callable" aria-hidden="true">#</a> 1、Runnable 与 Callable</h4><blockquote><p><code>Runnable</code> 接口不会返回结果或抛出检查异常，但是 <code>Callable</code>接口可以。所以，如果不需要返回结果或抛出异常推荐使用<code> Runnable</code>接口</p><p><code>Executors.callable()</code>可以实现 <code>Runnable</code>转为<code>Callable</code></p></blockquote><h4 id="_2、execute-与-submit" tabindex="-1"><a class="header-anchor" href="#_2、execute-与-submit" aria-hidden="true">#</a> 2、execute() 与 submit()</h4><blockquote><p><code>execute()</code> 方法用于提交不需要返回值的任务。所以无法判断任务是否被线程池执行成功与否。</p><p><code>submit()</code>方法用于提交需要返回值的任务。线程池会返回一个<code>Future</code>类型的对象，通过这个对象可以判断任务是否执行成功，并可以通过<code>Future</code>的<code>get()</code>方法获取返回值，<code>get()</code>方法会阻塞当前线程直到任务完成，<code>get(long timeout, TimeUnit unit)</code>方法会阻塞当前线程一段时间后立即返回，这时候可能任务没有执行完。</p></blockquote><h4 id="_3、shutdown-与-showdownnow" tabindex="-1"><a class="header-anchor" href="#_3、shutdown-与-showdownnow" aria-hidden="true">#</a> 3、shutdown() 与 showdownNow()</h4><blockquote><p><code>shutdown()</code>: 关闭线程池，线程池的状态变为 <code>SHUTDOWN</code>。线程池不再接受新任务，但是队列里的任务得执行完毕。</p><p><code>shutdownNow()</code>: 关闭线程池，线程池的状态变为 <code>STOP</code>。线程池会终止当前正在运行的任务，并停止处理派对的任务并返回正在等待执行的 list</p></blockquote><h4 id="_4、isterminated-与-isshutdown" tabindex="-1"><a class="header-anchor" href="#_4、isterminated-与-isshutdown" aria-hidden="true">#</a> 4、isTerminated() 与 isShutdown()</h4><blockquote><p><code>isShutDown()</code>: 当调用<code>shutDown()</code>方法后返回true</p><p><code>isTerminated()</code>: 当调用<code>shutDown()</code>方法后，并且所有提交的任务完成后返回true</p></blockquote><h3 id="五、几种常见的线程池" tabindex="-1"><a class="header-anchor" href="#五、几种常见的线程池" aria-hidden="true">#</a> 五、几种常见的线程池</h3><h4 id="_1、fixed-thread-pool" tabindex="-1"><a class="header-anchor" href="#_1、fixed-thread-pool" aria-hidden="true">#</a> 1、Fixed Thread Pool</h4><blockquote><p>可重用固定线程数的线程池</p></blockquote><h5 id="_1-1-执行任务过程" tabindex="-1"><a class="header-anchor" href="#_1-1-执行任务过程" aria-hidden="true">#</a> 1.1 执行任务过程</h5><ol><li>如果当前运行数量小于<code>corePoolSize</code>，如果再来新任务的话，就创建新的线程来执行任务；</li><li>当前运行线程数等于<code>corePoolSize</code>后，如果再来新任务，就加入<code>LinkedBlockQueue</code></li><li>线程池中的线程执行完手头任务后，会循环反复从<code>LinkedBlockQueue</code>中获取任务执行</li></ol><h5 id="_1-2-为什么不推荐使用该线程池" tabindex="-1"><a class="header-anchor" href="#_1-2-为什么不推荐使用该线程池" aria-hidden="true">#</a> 1.2 为什么不推荐使用该线程池</h5><p><code>FixedThreadPool</code> 使用无界队列 <code>LinkedBlockingQueue</code>（队列的容量为 <code>Intger.MAX_VALUE</code>）作为线程池的工作队列会对线程池带来如下影响 ：</p><ol><li>当线程池中的线程数达到 <code>corePoolSize</code> 后，新任务将在无界队列中等待，因此线程池中的线程数不会超过 <code>corePoolSize</code>；</li><li>由于使用无界队列时 <code>maximumPoolSize</code> 将是一个无效参数，因为不可能存在任务队列满的情况。所以，通过创建 <code>FixedThreadPool</code>的源码可以看出创建的 <code>FixedThreadPool</code> 的 <code>corePoolSize</code> 和 <code>maximumPoolSize</code> 被设置为同一个值。</li><li>由于 1 和 2，使用无界队列时 <code>keepAliveTime</code> 将是一个无效参数；</li><li>运行中的 <code>FixedThreadPool</code>（未执行 <code>shutdown()</code>或 <code>shutdownNow()</code>）不会拒绝任务，在任务比较多的时候会导致 <code>OOM</code>（内存溢出）。</li></ol><h5 id="_5-2-singlethreadexecutor" tabindex="-1"><a class="header-anchor" href="#_5-2-singlethreadexecutor" aria-hidden="true">#</a> 5.2 <code>SingleThreadExecutor </code></h5><blockquote><p><code>SingleThreadExecutor</code> 只有一个线程的线程池。</p><p>该线程池极端情况下也会导致 <code>OOM</code></p></blockquote><h5 id="_5-3-cachedthreadpool" tabindex="-1"><a class="header-anchor" href="#_5-3-cachedthreadpool" aria-hidden="true">#</a> 5.3 <code>CachedThreadPool</code></h5><blockquote><p><code>CachedThreadPool</code> 是一个会根据需要创建新线程的线程池</p><p>该线程池极端情况下也会导致 <code>OOM</code></p></blockquote><hr><h3 id="六、线程池大小确定" tabindex="-1"><a class="header-anchor" href="#六、线程池大小确定" aria-hidden="true">#</a> 六、线程池大小确定</h3><blockquote><p><strong>如果我们设置线程数量太大，大量线程可能会同时在争取 CPU 资源，这样会导致大量的上下文切换，从而增加线程的执行时间，影响了整体执行效率。</strong></p></blockquote><blockquote><p>上下文切换：</p><p>多线程编程中一般线程的个数都大于 CPU 核心的个数，而一个 CPU 核心在任意时刻只能被一个线程使用，为了让这些线程都能得到有效执行，CPU 采取的策略是为每个线程分配时间片并轮转的形式。当一个线程的时间片用完的时候就会重新处于就绪状态让给其他线程使用，这个过程就属于一次上下文切换。</p><p>概括来说就是：当前任务在执行完 CPU 时间片切换到另一个任务之前会先保存自己的状态，以便下次再切换回这个任务时，可以再加载这个任务的状态。<strong>任务从保存到再加载的过程就是一次上下文切换</strong>。</p><p>上下文切换通常是计算密集型的。也就是说，它需要相当可观的处理器时间，在每秒几十上百次的切换中，每次切换都需要纳秒量级的时间。所以，上下文切换对系统来说意味着消耗大量的 CPU 时间，事实上，可能是操作系统中时间消耗最大的操作。</p><p>Linux 相比与其他操作系统（包括其他类 Unix 系统）有很多的优点，其中有一项就是，其上下文切换和模式切换的时间消耗非常少。</p></blockquote><p>有一个简单并且适用面比较广的公式：</p><ul><li><strong>CPU 密集型任务(N+1)：</strong> 这种任务消耗的主要是 CPU 资源，可以将线程数设置为 N（CPU 核心数）+1，比 CPU 核心数多出来的一个线程是为了防止线程偶发的缺页中断，或者其它原因导致的任务暂停而带来的影响。一旦任务暂停，CPU 就会处于空闲状态，而在这种情况下多出来的一个线程就可以充分利用 CPU 的空闲时间。</li><li><strong>I/O 密集型任务(2N)：</strong> 这种任务应用起来，系统会用大部分的时间来处理 I/O 交互，而线程在处理 I/O 的时间段内不会占用 CPU 来处理，这时就可以将 CPU 交出给其它线程使用。因此在 I/O 密集型任务的应用中，我们可以多配置一些线程，具体的计算方法是 2N。</li></ul>`,53),l=[n];function t(i,r){return o(),c("div",null,l)}const u=e(a,[["render",t],["__file","xianchengchi.html.vue"]]);export{u as default};
